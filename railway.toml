[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd /app && python -m web.app"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
